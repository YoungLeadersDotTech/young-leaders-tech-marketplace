# opencode-sync runtime / build artefacts - never commit
.skills-build/
.toast-build/
.opencode-sync/manifest.json

# Generated OpenCode artefacts for this checkout - never commit.
# .opencode/agent/*.md are generated by sync_agents.py from canonical sources
# (often a separate/private repo) and carry machine-specific absolute paths;
# regenerate them, don't track them. .opencode/ also holds the local plugin
# node_modules. Never tracked in this repo's history.
.opencode/

# Private docs
docs/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
